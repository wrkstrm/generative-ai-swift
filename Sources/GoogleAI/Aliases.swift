import Foundation
import WrkstrmNetworking

// Backwards-compatible aliases used by tests and public API samples.
public typealias RequestOptions = HTTP.Request.Options
public typealias CountTokensResponse = CountTokens.Response
// Intentionally no alias for GenerateContentRequest; a compatibility struct is provided instead.
